.tabbable.tabs
  %ul.nav.nav-tabs
    %li{"class" => current_tab == "users" ? "active" : ""}
      = link_to icon_link(:heart) + " 用户管理", admin_users_path
    %li{"class" => current_tab == "pages" ? "active" : ""}
      = link_to icon_link(:heart) + " 页面管理", admin_pages_path
